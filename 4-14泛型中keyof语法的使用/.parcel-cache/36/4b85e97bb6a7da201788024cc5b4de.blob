class Teacher {
  constructor(info) {
    this.info = info;
  }

  getInfo(key) {
    return this.info[key];
  }

}

const teacher = new Teacher({
  name: 'xiaoxiaoweii',
  age: 100,
  gender: 'male'
});
const test = teacher.getInfo('name');
console.log(test);